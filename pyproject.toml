[project]
name = "review-radar"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "crawl4ai>=0.8.6",
    "prefect>=3.7.1",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]

[tool.pyright]
extraPaths = ["."]